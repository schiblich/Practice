var lista = ["10","Marula","Noni","Pequi","Azeitona","Jaca","Uva","Tangerina","Lucuma","Figo","Cajá"];
let valLista = "lista.length";

function retornaPrimeiroElemento(lista) {
// não altere a linha acima
let valLista; var resultado; vallista = lista.length; res1 = lista[0];
  if (valLista > 0) {
    resultado = res1;
  }else{
     resultado = "Array vazia"
 }
/*
 * Complete o código atribuindo na variável `resultado`
 * o primeiro elemento do array `lista` se o array não estiver vazio,
 * ou a string "array vazio" se o array `lista` estiver vazio.
 */
 
// não altere as linhas abaixo
  return resultado;
}
retornaPrimeiroElemento()


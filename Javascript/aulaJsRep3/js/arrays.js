let valores = [5,8,3,9,10];
/*immprimmir  a  array
//  console.log('Nosso  vetor tem:' + ' ' + num.length + ' ' + 'elemmentos');
for (var pos=0; pos<=valores.length;pos++){
    console.log(valores[pos]);
}*/ 
for (let pos in valores){
    console.log(valores[pos])
}